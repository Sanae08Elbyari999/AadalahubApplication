import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    container: {
      padding:80,
      marginTop: 2,
      resizeMode: 'cover',
    },
    
  });
  export default styles;